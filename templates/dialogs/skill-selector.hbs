<div class="sdp-dialog">

  <label>{{localize "SDP.Skill"}}</label>

  <select id="skill-select">
    {{#each skills}}
      <option value="{{this.id}}">
        {{this.name}}
      </option>
    {{/each}}
  </select>

  <button id="confirm">
    {{localize "SDP.Add"}}
  </button>

</div>