<div class="sdp-dialog">

  <div class="dialog-content">
    {{{content}}}
  </div>

  <div class="dialog-buttons">

    {{#each buttons as |btn key|}}
      <button data-button="{{key}}">
        {{btn.label}}
      </button>
    {{/each}}

  </div>

</div>