<div class="tab-magic">

  {{#if (eq actor.type "creature")}}

  {{#if passiveAbilities.length}}
  <!-- PASSIVE ABILITIES -->
  <h3>{{localize "SDP.PassiveAbilities"}}</h3>

  <table class="spells-table abilities-table passive-abilities-table">
    <thead>
      <tr>
        <th></th>
        <th>{{localize "SDP.Name"}}</th>
        <th>{{localize "SDP.Actions"}}</th>
      </tr>
    </thead>

    <tbody>
      {{#each passiveAbilities as |ability|}}
<tr class="spell-row ability-row" data-item-id="{{ability.id}}">

  <td class="spell-img-cell">
  <img src="{{ability.img}}" title="{{ability.displayName}}">
</td>

  <td
    class="spell-name ability-toggle"
    data-action="toggleAbilityDetails"
    data-item-id="{{ability.id}}"
  >
    {{ability.displayName}}
  </td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{ability.id}}">✏️</button>
    <button data-action="deleteItem" data-item-id="{{ability.id}}">🗑️</button>
  </td>

</tr>

<tr class="spell-details" data-details="{{ability.id}}" style="display:none;">
  <td colspan="3">

    <div class="spell-detail-box">

  <p><strong>{{localize "SDP.Range"}}:</strong> {{ability.system.range.value}}</p>

  <p>
  <strong>{{localize "SDP.Duration"}}:</strong>
  {{#if ability.system.concentration.value}}
    {{localize "SDP.Concentration"}}
  {{else}}
    {{ability.system.duration.value}}
    {{ability.system.duration.type}}
  {{/if}}
</p>
{{#if ability.system.concentration.value}}
{{#if ability.system.maintainRange.value}}
  <p><strong>{{localize "SDP.MaintainRange"}}:</strong> {{ability.system.maintainRange.value}}</p>
{{/if}}
{{/if}}

  <p>
  <strong>{{localize "SDP.Target"}}:</strong>
  {{#if ability.system.aoe.value}}
    {{localize "SDP.AreaOfEffect"}} ({{localize "SDP.Radius"}}: {{ability.system.radius.value}})
  {{else}}
    {{ability.system.target.value}} {{localize "SDP.Targets"}}
  {{/if}}
</p>

  <p>
  <strong>{{localize "SDP.Damage"}}:</strong>
    {{ability.system.damage.base.value}}
    {{ability.system.damage.dice.value}}
  </p>

  <p class="spell-description">
  {{ability.system.description}}
</p>
</div>

  </td>
</tr>

  {{/each}}
</tbody>
  </table>
  {{/if}}

  {{#if activeAbilities.length}}
  <!-- ACTIVE ABILITIES -->
  <h3>{{localize "SDP.ActiveAbilities"}}</h3>

  <table class="spells-table abilities-table">
    <thead>
      <tr>
        <th></th>
        <th>{{localize "SDP.Name"}}</th>
        <th>{{localize "SDP.Mana"}}</th>
        <th>{{localize "SDP.Actions"}}</th>
      </tr>
    </thead>

    <tbody>
      {{#each activeAbilities as |ability|}}
<tr class="spell-row ability-row" data-item-id="{{ability.id}}">

  <td class="spell-img-cell">
  <img src="{{ability.img}}" title="{{ability.displayName}}">
</td>

  <td
    class="spell-name roll-ability-name"
    data-action="useAbility"
    data-item-id="{{ability.id}}"
  >
    {{ability.displayName}}
  </td>

  <td>
    {{ability.system.power.value}}
  </td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{ability.id}}">✏️</button>
    <button data-action="deleteItem" data-item-id="{{ability.id}}">🗑️</button>
  </td>

</tr>

<tr class="spell-details" data-details="{{ability.id}}" style="display:none;">
  <td colspan="4">

    <div class="spell-detail-box">

  <p><strong>{{localize "SDP.Range"}}:</strong> {{ability.system.range.value}}</p>

  <p>
  <strong>{{localize "SDP.Duration"}}:</strong>
  {{#if ability.system.concentration.value}}
    {{localize "SDP.Concentration"}}
  {{else}}
    {{ability.system.duration.value}}
    {{ability.system.duration.type}}
  {{/if}}
</p>
{{#if ability.system.concentration.value}}
{{#if ability.system.maintainRange.value}}
  <p><strong>{{localize "SDP.MaintainRange"}}:</strong> {{ability.system.maintainRange.value}}</p>
{{/if}}
{{/if}}

  <p>
  <strong>{{localize "SDP.Target"}}:</strong>
  {{#if ability.system.aoe.value}}
    {{localize "SDP.AreaOfEffect"}} ({{localize "SDP.Radius"}}: {{ability.system.radius.value}})
  {{else}}
    {{ability.system.target.value}} {{localize "SDP.Targets"}}
  {{/if}}
</p>

  <p>
  <strong>{{localize "SDP.Damage"}}:</strong>
    {{ability.system.damage.base.value}}
    {{ability.system.damage.dice.value}}
  </p>

  <p class="spell-description">
  {{ability.system.description}}
</p>
</div>

  </td>
</tr>

  {{/each}}
</tbody>
  </table>
  {{/if}}

  {{/if}}

  <!-- MINOR SPELLS -->
  <h3>{{localize "SDP.MinorSpells"}}</h3>

  <table class="spells-table">
    <thead>
      <tr>
        <th></th>
        <th>{{localize "SDP.Name"}}</th>
<th>{{localize "SDP.Mana"}}</th>
<th>{{localize "SDP.Memory"}}</th>
<th>{{localize "SDP.Actions"}}</th>
      </tr>
    </thead>

    <tbody>
      {{#each spellsMinor as |spell|}}
<tr class="spell-row" data-item-id="{{spell.id}}">

  <td class="spell-img-cell">
  <img src="{{spell.img}}" title="{{spell.name}}">
</td>

  <td 
    class="spell-name roll-spell-name" 
    data-action="rollSpell"
    data-item-id="{{spell.id}}"
  >
    {{spell.name}}
  </td>

  <td>
    {{spell.system.power.value}}
  </td>

  <td>
    <input type="checkbox"
      data-action="toggleSpellMemory"
      data-item-id="{{spell.id}}"
      {{checked spell.system.memorized.value}}>
  </td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{spell.id}}">✏️</button>
    <button data-action="deleteItem" data-item-id="{{spell.id}}">🗑️</button>
  </td>

</tr>

<!-- 🔽 DEPLIANT -->
<tr class="spell-details" data-details="{{spell.id}}" style="display:none;">
  <td colspan="5">

    <div class="spell-detail-box">

  <p><strong>Magic:</strong> {{spell.system.magic.value}}</p>
  <p><strong>Range:</strong> {{spell.system.range.value}}</p>

  <!-- ========================= -->
  <!-- DURATION -->
  <!-- ========================= -->
  <p>
  <strong>Duration:</strong>
  {{#if spell.system.concentration.value}}
    Concentration
  {{else}}
    {{spell.system.duration.value}}
    {{spell.system.duration.type}}
    {{#if spell.system.duration.extendable}} (Extendable){{/if}}
  {{/if}}
</p>
{{#if spell.system.concentration.value}}
{{#if spell.system.maintainRange.value}}
  <p><strong>{{localize "SDP.MaintainRange"}}:</strong> {{spell.system.maintainRange.value}}</p>
{{/if}}
{{/if}}

  <!-- ========================= -->
  <!-- TARGET -->
  <!-- ========================= -->
  <p>
  <strong>Target:</strong>
  {{#if spell.system.aoe.value}}
    Area of Effect (Radius: {{spell.system.radius.value}})
  {{else}}
    {{spell.system.target.value}} target(s)
  {{/if}}
</p>

  <!-- ========================= -->
  <!-- DAMAGE -->
  <!-- ========================= -->
  <p><strong>Damage:</strong>
    {{spell.system.damage.base.value}}
    {{spell.system.damage.dice.value}}
  </p>

  <!-- ========================= -->
  <!-- OVERCAST SPECIAL -->
  <!-- ========================= -->
{{#each spell.system.overcastSpecialEffects.value as |effect|}}
  {{#if @first}}
    <div class="overcast-line">
      <strong>Overcast Special:</strong>
      <ul>
  {{/if}}

  <li>
    <strong>{{effect.label}}</strong> : {{effect.value}}
  </li>

  {{#if @last}}
      </ul>
    </div>
  {{/if}}
{{/each}}

  <!-- ========================= -->
  <!-- DESCRIPTION -->
  <!-- ========================= -->
  <p class="spell-description">
  {{spell.system.description}}
</p>

</div>

  </td>
</tr>
      {{/each}}
    </tbody>
  </table>


  <!-- ADVANCED SPELLS -->
  <h3>{{localize "SDP.AdvancedSpells"}}</h3>

  <table class="spells-table">
    <thead>
      <tr>
        <th></th>
        <th>{{localize "SDP.Name"}}</th>
<th>{{localize "SDP.Mana"}}</th>
<th>{{localize "SDP.Memory"}}</th>
<th>{{localize "SDP.Actions"}}</th>
      </tr>
    </thead>

<tbody>
  {{#each spellsAdvanced as |spell|}}

<tr class="spell-row" data-item-id="{{spell.id}}">

  <td class="spell-img-cell">
  <img src="{{spell.img}}" title="{{spell.name}}">
</td>

  <td 
    class="spell-name roll-spell-name" 
    data-action="rollSpell"
    data-item-id="{{spell.id}}"
  >
    {{spell.name}}
  </td>

  <td>
    {{spell.system.power.value}}
  </td>

  <td>
    <input type="checkbox"
      data-action="toggleSpellMemory"
      data-item-id="{{spell.id}}"
      {{checked spell.system.memorized.value}}>
  </td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{spell.id}}">✏️</button>
    <button data-action="deleteItem" data-item-id="{{spell.id}}">🗑️</button>
  </td>

</tr>

<!-- 🔽 DEPLIANT -->
<tr class="spell-details" data-details="{{spell.id}}" style="display:none;">
  <td colspan="5">

    <div class="spell-detail-box">

  <p><strong>Magic:</strong> {{spell.system.magic.value}}</p>
  <p><strong>Range:</strong> {{spell.system.range.value}}</p>

  <!-- ========================= -->
  <!-- DURATION -->
  <!-- ========================= -->
  <p>
  <strong>Duration:</strong>
  {{#if spell.system.concentration.value}}
    Concentration
  {{else}}
    {{spell.system.duration.value}}
    {{spell.system.duration.type}}
    {{#if spell.system.duration.extendable}} (Extendable){{/if}}
  {{/if}}
</p>
{{#if spell.system.concentration.value}}
{{#if spell.system.maintainRange.value}}
  <p><strong>{{localize "SDP.MaintainRange"}}:</strong> {{spell.system.maintainRange.value}}</p>
{{/if}}
{{/if}}

  <!-- ========================= -->
  <!-- TARGET -->
  <!-- ========================= -->
  <p>
  <strong>Target:</strong>
  {{#if spell.system.aoe.value}}
    Area of Effect (Radius: {{spell.system.radius.value}})
  {{else}}
    {{spell.system.target.value}} target(s)
  {{/if}}
</p>

  <!-- ========================= -->
  <!-- DAMAGE -->
  <!-- ========================= -->
  <p><strong>Damage:</strong>
    {{spell.system.damage.base.value}}
    {{spell.system.damage.dice.value}}
  </p>

  <!-- ========================= -->
  <!-- OVERCAST SPECIAL -->
  <!-- ========================= -->
{{#each spell.system.overcastSpecialEffects.value as |effect|}}
  {{#if @first}}
    <div class="overcast-line">
      <strong>Overcast Special:</strong>
      <ul>
  {{/if}}

  <li>
    <strong>{{effect.label}}</strong> : {{effect.value}}
  </li>

  {{#if @last}}
      </ul>
    </div>
  {{/if}}
{{/each}}

  <!-- ========================= -->
  <!-- DESCRIPTION -->
  <!-- ========================= -->
  <p class="spell-description">
  {{spell.system.description}}
</p>

</div>

  </td>
</tr>

  {{/each}}
</tbody>
  </table>


  <!-- SUPERIOR SPELLS -->
  <h3>{{localize "SDP.SuperiorSpells"}}</h3>

  <table class="spells-table">
    <thead>
      <tr>
        <th></th>
        <th>{{localize "SDP.Name"}}</th>
<th>{{localize "SDP.Mana"}}</th>
<th>{{localize "SDP.Memory"}}</th>
<th>{{localize "SDP.Actions"}}</th>
      </tr>
    </thead>

    <tbody>
  {{#each spellsSuperior as |spell|}}

<tr class="spell-row" data-item-id="{{spell.id}}">

  <td class="spell-img-cell">
  <img src="{{spell.img}}" title="{{spell.name}}">
</td>

  <td 
    class="spell-name roll-spell-name" 
    data-action="rollSpell"
    data-item-id="{{spell.id}}"
  >
    {{spell.name}}
  </td>

  <td>
    {{spell.system.power.value}}
  </td>

  <td>
    <input type="checkbox"
      data-action="toggleSpellMemory"
      data-item-id="{{spell.id}}"
      {{checked spell.system.memorized.value}}>
  </td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{spell.id}}">✏️</button>
    <button data-action="deleteItem" data-item-id="{{spell.id}}">🗑️</button>
  </td>

</tr>

<!-- 🔽 DEPLIANT -->
<tr class="spell-details" data-details="{{spell.id}}" style="display:none;">
  <td colspan="5">

    <div class="spell-detail-box">

  <p>
  <strong>{{localize "SDP.Magic"}}:</strong>
  {{spell.system.magic.value}}
</p>
  <p>
  <strong>{{localize "SDP.Range"}}:</strong>
  {{spell.system.range.value}}
</p>

  <!-- ========================= -->
  <!-- DURATION -->
  <!-- ========================= -->
  <p>
  <strong>{{localize "SDP.Duration"}}:</strong>
  {{#if spell.system.concentration.value}}
    {{localize "SDP.Concentration"}}
  {{else}}
    {{spell.system.duration.value}}
    {{spell.system.duration.type}}
    {{#if spell.system.duration.extendable}} ({{localize "SDP.Extendable"}}){{/if}}
  {{/if}}
</p>
{{#if spell.system.concentration.value}}
{{#if spell.system.maintainRange.value}}
  <p><strong>{{localize "SDP.MaintainRange"}}:</strong> {{spell.system.maintainRange.value}}</p>
{{/if}}
{{/if}}

  <!-- ========================= -->
  <!-- TARGET -->
  <!-- ========================= -->
  <p>
  <strong>{{localize "SDP.Target"}}:</strong>
  {{#if spell.system.aoe.value}}
    {{localize "SDP.AreaOfEffect"}}
({{localize "SDP.Radius"}}:
{{spell.system.radius.value}})
  {{else}}
    {{spell.system.target.value}}
{{localize "SDP.Targets"}}
  {{/if}}
</p>

  <!-- ========================= -->
  <!-- DAMAGE -->
  <!-- ========================= -->
  <p>
  <strong>{{localize "SDP.Damage"}}:</strong>
    {{spell.system.damage.base.value}}
    {{spell.system.damage.dice.value}}
  </p>

  <!-- ========================= -->
  <!-- OVERCAST SPECIAL -->
  <!-- ========================= -->
{{#each spell.system.overcastSpecialEffects.value as |effect|}}
  {{#if @first}}
    <div class="overcast-line">
      <strong>{{localize "SDP.SpecialOvercast"}}:</strong>
      <ul>
  {{/if}}

  <li>
    <strong>{{effect.label}}</strong> : {{effect.value}}
  </li>

  {{#if @last}}
      </ul>
    </div>
  {{/if}}
{{/each}}
  <!-- ========================= -->
  <!-- DESCRIPTION -->
  <!-- ========================= -->
  <p class="spell-description">
  {{spell.system.description}}
</p>
</div>

  </td>
</tr>

  {{/each}}
</tbody>
  </table>

</div>
