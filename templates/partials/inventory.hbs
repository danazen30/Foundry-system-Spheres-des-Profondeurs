<div class="tab-inventory">
  <div class="inventory-scroll">

<h2>Melee Weapons</h2>

<table class="weapons-table melee-table">
<thead>
<tr>
  <th></th>
<th>Name</th>
<th>Equip</th>
<th>Offhand</th>
<th>Defense</th>
<th>Qty</th>
<th>Weight</th>
<th></th>
</tr>
</thead>

<tbody>

{{#each meleeWeapons as |item|}}

<tr class="weapon-row weapon-clickable" data-item-id="{{item.id}}">

  <td>
  <img src="{{item.img}}" class="item-img">
</td>

<td class="weapon-toggle" data-item-id="{{item.id}}">
 <span class="weapon-name">{{item.name}}</span>
</td>

<td>
  <input
    type="checkbox"
    data-action="toggleWeaponEquip"
    data-item-id="{{item.id}}"
    {{checked item.system.equipped}}
  >
</td>

<td>
  <input
    type="checkbox"
    data-action="toggleOffhand"
    data-item-id="{{item.id}}"
    {{checked item.system.offhand}}
  >
</td>

<td>
  <input type="checkbox"
         class="toggle-defense-weapon"
         data-item-id="{{item._id}}"
         {{checked item.system.isDefenseWeapon}}>
</td>

<td>
  {{item.system.quantity.value}}
</td>

<td>
  {{item.system.encumbrance.value}}
</td>

<td class="item-actions">
  <button data-action="editItem" data-item-id="{{item.id}}">✏️</button>
  <button data-action="deleteItem" data-item-id="{{item.id}}">🗑️</button>
</td>

</tr>

<tr class="weapon-details" data-details="{{item.id}}" style="display: none;">

<td colspan="100%">

  <div class="weapon-details-content">

    <!-- 🔥 LIGNE PRINCIPALE -->
    <div class="weapon-line main-line">

      <div>
        <strong>Skill:</strong> {{item.displaySkill}}
      </div>

      <div>
        <strong>Damage:</strong>
        {{item.system.damage}}
        {{#if item.system.damageDice}}
          + {{item.system.damageDice}}
        {{/if}}
      </div>

      <div class="traits-inline">
        <strong>Traits:</strong>

        {{#each item.displayTraits}}
          <span class="trait-tag {{this.type}} {{#if this.disabled}}trait-disabled{{/if}}">
            {{this.label}}
          </span>
        {{/each}}
      </div>

    </div>

    <!-- 🔽 DESCRIPTION -->
    {{#if item.system.description}}
      <div class="weapon-desc">
        {{item.system.description}}
      </div>
    {{/if}}

  </div>

</td>

</tr>

{{/each}}

</tbody>
</table>

<h2>Ranged Weapons</h2>

<table class="weapons-table ranged-table">
<thead>
<tr>
  <th></th>
<th>Name</th>
<th>Ammo</th>
<th>Equip</th>
<th>Offhand</th>
<th>Qty</th>
<th>Weight</th>
<th></th>
</tr>
</thead>

<tbody>

{{#each rangedWeapons as |item|}}

<tr class="weapon-row weapon-clickable" data-item-id="{{item.id}}">

  <td>
  <img src="{{item.img}}" class="item-img">
</td>

<td class="weapon-toggle" data-item-id="{{item.id}}">
  <span class="weapon-name">{{item.name}}</span>
</td>

<!-- 🔫 AMMO (déplacé ici) -->
<td>
  <div class="ammo-row">

    <select data-action="selectAmmo"
            data-item-id="{{item._id}}">

      <option value="">-- Aucun --</option>

      {{#each item.compatibleAmmo}}
        <option value="{{this._id}}"
          {{#if (eq this._id ../system.currentAmmo)}}selected{{/if}}>
          {{this.name}} ({{this.system.quantity.value}})
        </option>
      {{/each}}

    </select>

    {{#if item.hasReload}}
      <input type="checkbox"
             class="reload-checkbox"
             data-action="toggleLoaded"
             data-item-id="{{item._id}}"
             {{checked item.system.loaded}}>
    {{/if}}

  </div>
</td>

<td>
  <input
    type="checkbox"
    data-action="toggleWeaponEquip"
    data-item-id="{{item.id}}"
    {{checked item.system.equipped}}
  >
</td>

<td>
  <input
    type="checkbox"
    data-action="toggleOffhand"
    data-item-id="{{item.id}}"
    {{checked item.system.offhand}}
  >
</td>

<td>
  {{item.system.quantity.value}}
</td>

<td>
  {{item.system.encumbrance.value}}
</td>

<td class="item-actions">
  <button data-action="editItem" data-item-id="{{item.id}}">✏️</button>
  <button data-action="deleteItem" data-item-id="{{item.id}}">🗑️</button>
</td>

</tr>

<tr class="weapon-details" data-details="{{item.id}}" style="display: none;">

<td colspan="100%">

  <div class="weapon-details-content">

    <!-- 🔥 LIGNE PRINCIPALE -->
    <div class="weapon-line main-line">

      <div>
        <strong>Range:</strong>
        {{item.system.range}}

        {{#if item.system.currentAmmo}}
          {{#each item.compatibleAmmo as |ammo|}}
            {{#if (eq ammo.id item.system.currentAmmo)}}
              ({{ammo.system.rangeModifier}})
            {{/if}}
          {{/each}}
        {{/if}}
      </div>

      <div>
        <strong>Skill:</strong> {{item.displaySkill}}
      </div>

      <div>
        <strong>Damage:</strong>
        {{item.system.damage}}

        {{#if item.system.damageDice}}
          + {{item.system.damageDice}}
        {{/if}}

        {{#if item.system.currentAmmo}}
          {{#each item.compatibleAmmo as |ammo|}}
            {{#if (eq ammo.id item.system.currentAmmo)}}

              {{#if ammo.system.damage.base.value}}
                + {{ammo.system.damage.base.value}}
              {{/if}}

              {{#if ammo.system.damage.dice.value}}
                + {{ammo.system.damage.dice.value}}
              {{/if}}

            {{/if}}
          {{/each}}
        {{/if}}
      </div>

      <div class="traits-inline">
        <strong>Traits:</strong>

        {{#each item.displayTraits}}
          <span class="trait-tag {{this.type}} {{#if this.disabled}}trait-disabled{{/if}}">
            {{this.label}}
          </span>
        {{/each}}
      </div>

    </div>

    <!-- DESCRIPTION -->
    {{#if item.system.description}}
      <div class="weapon-desc">
        {{item.system.description}}
      </div>
    {{/if}}

  </div>

</td>

</tr>

{{/each}}

</tbody>
</table>

<h2>Ammunition</h2>

<table class="ammo-table">
<thead>
<tr>
  <th></th>
<th>Name</th>
<th>Group</th>
<th>Qty</th>
<th>Damage</th>
<th>Range Mod</th>
<th></th>
</tr>
</thead>

<tbody>

{{#each ammunition as |item|}}

<tr class="item-row" data-item-id="{{item.id}}">

  <td>
  <img src="{{item.img}}" class="item-img">
</td>

<td>{{item.name}}</td>

<td>{{item.system.weaponGroup}}</td>

<td>
  <input
    type="number"
    data-action="updateAmmoQty"
    data-item-id="{{item.id}}"
    value="{{item.system.quantity.value}}"
    min="0"
  >
</td>

<td>
  {{item.system.damage.base.value}}
  {{#if item.system.damage.dice.value}}
    + {{item.system.damage.dice.value}}
  {{/if}}
</td>

<td>{{item.system.rangeModifier}}</td>

<td class="item-actions">
  <button data-action="editItem" data-item-id="{{item.id}}">✏️</button>
  <button data-action="deleteItem" data-item-id="{{item.id}}">🗑️</button>
</td>

</tr>

{{/each}}

</tbody>
</table>

<h2>Armors</h2>

<table class="armors-table">

<thead>
<tr>
  <th></th>
<th>Name</th>
<th>Head</th>
<th>Body</th>
<th>L Arm</th>
<th>R Arm</th>
<th>L Leg</th>
<th>R Leg</th>
<th>Worn</th>
<th></th>
</tr>
</thead>

<tbody>

{{#each armors as |item|}}

<tr class="item-row armor-clickable" data-item-id="{{item.id}}">

  <td>
    <img src="{{item.img}}" class="item-img">
  </td>

  <!-- 👇 IMPORTANT -->
  <td class="armor-toggle" data-item-id="{{item.id}}">
    <span class="armor-name">{{item.name}}</span>
  </td>

  <td>{{item.system.AP.head}}</td>
  <td>{{item.system.AP.body}}</td>
  <td>{{item.system.AP.leftArm}}</td>
  <td>{{item.system.AP.rightArm}}</td>
  <td>{{item.system.AP.leftLeg}}</td>
  <td>{{item.system.AP.rightLeg}}</td>

  <td>
    <input
      type="checkbox"
      data-action="toggleArmor"
      data-item-id="{{item.id}}"
      {{checked item.system.worn.value}}>
  </td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{item.id}}">✏️</button>
    <button data-action="deleteItem" data-item-id="{{item.id}}">🗑️</button>
  </td>

</tr>

<!-- 🔽 DEPLIANT -->
<tr class="armor-details" data-details="{{item.id}}" style="display:none;">
  <td colspan="100%">

    <div class="armor-details-content">

      <!-- TRAITS ARMOR -->
      <div class="traits-inline">
  <strong>Traits:</strong>

  {{#each item.displayArmorTraits}}
    <span class="trait-tag {{this.type}}">
      {{this.label}}
      {{#if this.value}} ({{this.value}}){{/if}}
    </span>
  {{/each}}
</div>

      <!-- DESCRIPTION -->
      {{#if item.system.description}}
        <div class="armor-desc">
          {{item.system.description}}
        </div>
      {{/if}}

    </div>

  </td>
</tr>

{{/each}}

</tbody>

</table>

<h2>Clothing & Accessories</h2>

<table class="inventory-table">
<thead>
<tr>
  <th></th>
  <th>Name</th>
  <th>Equipped</th>
  <th></th>
</tr>
</thead>

<tbody>

{{#each clothing}}
<tr class="item-row" data-item-id="{{this.id}}">

  <td>
    <img src="{{this.img}}" class="item-img">
  </td>

  <td>{{this.name}}</td>

  <td>
    <input type="checkbox"
           data-item-id="{{this._id}}"
           data-action="toggleClothing"
           {{checked this.system.equipped}} />
  </td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{this.id}}">✏️</button>
    <button data-action="deleteItem" data-item-id="{{this.id}}">🗑️</button>
  </td>

</tr>
{{/each}}

</tbody>
</table>

<h2>Possessions</h2>

<table class="inventory-table">
  <tr>
    <th></th>
    <th>Name</th>
    <th>Qty</th>
    <th>Weight</th>
    <th></th>
  </tr>

  {{#each possessions as |item|}}
<tr class="item-row" data-item-id="{{item.id}}">

  <td>
  <img src="{{item.img}}" class="item-img">
</td>

  <td class="item-name">
    {{item.name}}
  </td>

  <td>
    <input type="number"
           value="{{item.system.quantity.value}}"
           data-item-id="{{item.id}}"
           data-action="updateItemQty"
           min="0">
  </td>

  <td>
    {{item.system.weight.value}}
  </td>

  <td>
    <a data-action="editItem" data-item-id="{{item.id}}">✏️</a>
    <a data-action="deleteItem" data-item-id="{{item.id}}">🗑️</a>
  </td>

</tr>
  {{/each}}
</table>

  <!-- CONTENT -->
<h2>Containers</h2>

{{#each containers as |container|}}

<table class="container-table">

<thead>
<tr>
  <th></th>
  <th>Name</th>
  <th>Qty</th>
  <th>Weight</th>
  <th>Capacity</th>
  <th></th>
</tr>
</thead>

<tbody>

<tr class="container-header-row" data-container-id="{{container.id}}">

  <td>
    <img src="{{container.img}}" class="item-img">
  </td>

<td>
  <div class="container-cell">
    <button data-action="toggleContainer">▶</button>
    <strong>{{container.name}}</strong>
  </div>
</td>

  <td>{{container.system.quantity.value}}</td>
  <td>{{container.system.encumbrance.value}}</td>

  <td>
    {{#with (lookup ../containerLoad container.id) as |load|}}
      {{#if container.system.capacity.value}}
        {{load}} / {{container.system.capacity.value}}
      {{else}}
        {{load}}
      {{/if}}
    {{/with}}
  </td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{container.id}}">✏️</button>
    <button data-action="deleteItem" data-item-id="{{container.id}}">🗑️</button>
  </td>

</tr>

<tr class="container-content-row" data-container="{{container.id}}" style="display:none;">
<td colspan="100%">
  <div class="container-inner-wrapper">
    <table class="container-table inner">

<thead>
<tr>
  <th></th>
  <th>Name</th>
  <th>Qty</th>
  <th>Weight</th>
  <th>Type</th>
  <th></th>
</tr>
</thead>

<tbody>

{{#each (lookup ../containerMap container.id) as |item|}}

<tr class="item-row" draggable="true" data-item-id="{{item.id}}">

  <td><img src="{{item.img}}" class="item-img"></td>
  <td>{{item.name}}</td>
  <td>{{item.system.quantity.value}}</td>
  <td>{{item.system.encumbrance.value}}</td>
  <td>{{item.type}}</td>

  <td class="item-actions">
    <button data-action="editItem" data-item-id="{{item.id}}">✏️</button>
    <button data-action="removeFromContainer" data-item-id="{{item.id}}">↩</button>
    <button data-action="deleteItem" data-item-id="{{item.id}}">🗑️</button>
  </td>

</tr>

{{/each}}

</tbody>
</table>

</td>
</tr>

</tbody>
</table>

{{/each}}

  </div>
</div>